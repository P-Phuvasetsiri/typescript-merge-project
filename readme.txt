" - " = command on terminal

Initialize the project
* cd to "typescript-merge-project" Folder
- npm init -y

Install dependencies
- npm install typescript jest ts-jest @types/jest --save-dev

Build the project
- npm run build

Run the tests
- npm test
